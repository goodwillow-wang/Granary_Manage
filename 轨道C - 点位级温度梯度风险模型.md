产品需求文档 (PRD)：轨道C - 点位级温度梯度风险模型
文档版本	V2.0	状态	定稿
创建日期	2025年9月22日	最后修改	2025年9月23日
创建人	Gemini (AI 技术专家)	审核人	(待审核)
修订历史
版本	修改日期	修改人	主要变更内容
V2.0	2025-09-23	Gemini	1. 明确水平梯度正方向为“沿径向向外”。<br>2. 采用新的四级风险等级划分标准。<br>3. 整合关联文档内容，增强文档的自包含性。<br>4. 确认采用点位级独立风险评分机制。
V1.0	2025-09-22	Gemini	初始草案，定义了从“仓级”到“点位级”梯度的核心重构需求。
1. 项目概述
1.1 项目背景与目标
本项目旨在对现有“大型粮仓结露预警智能系统”进行关键升级。系统当前已具备轨道A（顶空温差法）与轨道B（ERH湿度校验）的预警能力。为捕捉由粮堆内部水汽迁移（Moisture Migration）引发的深层、早期结露风险，引入了轨道C（温度梯度风险模型）。
实践证明，将整个粮仓的温度梯度简化为单一宏观值的初代模型（V1.0）无法满足生产实际需求。本次升级（V2.0）的核心目标是将轨道C重构为一个精细化的“场”模型，为粮仓内每一个测温点独立计算其空间位置的温度梯度和风险等级，从而实现对潜在风险的精准定位。
1.2 系统架构集成
本需求将在现有技术栈中实现。系统的核心架构如下：
数据处理层: n8n自动化工作流，负责定时执行数据采集、风险计算与持久化。本PRD的核心逻辑将在n8n的Compute_TrackC Code节点中实现。
数据中心: PostgreSQL数据库，结合TimescaleDB（时序）和PostGIS（空间）扩展。所有计算结果将写入granary_risk_snapshot表。
可视化层: Python Plotly Dash应用，负责从数据库读取风险快照并进行三维可视化展示。本次升级将为可视化提供更丰富、更精细的数据源。
2. 功能需求详述
2.1 核心功能：点位级梯度计算
系统必须为每一次风险快照中的每一个测温点（由ts和sensor_id唯一确定）计算两个独立的温度梯度值：grad_v (垂直) 和 grad_h (水平)。
2.1.1 定义与约定
测温点: P(i, j)，代表第 j 号测温电缆上的第 i 层测温点。
温度值: T(i, j)，点 P(i, j) 的摄氏温度读数。
垂直间距: h，同一电缆上相邻测温点的垂直距离（米），可从配置中读取。
方向定义:
垂直方向 (Z轴): 以向上为正。正梯度值表示上层温度高于下层。
水平方向 (径向): 以沿半径向外为正。此约定基于“粮堆中心温度通常低于靠壁区域”的普遍物理现象，使得大部分梯度值为正，便于解读。正梯度值表示外圈温度高于内圈。
2.1.2 垂直温度梯度 (grad_v) 计算逻辑
对于同一电缆 j 上的测温点 P(i, j)：
中间点 (1 < i < n): 采用中心差分法，以获得平滑且准确的梯度估计。
\text{grad_v}(i, j) = \frac{T(i+1, j) - T(i-1, j)}{2h}
边界点 (i=1, i=n): 采用邻近点的梯度值进行填充，以确保数据完整性。
最底层 (i=1): grad_v(1, j) = grad_v(2, j)
最顶层 (i=n): grad_v(n, j) = grad_v(n-1, j)
2.1.3 水平温度梯度 (grad_h) 计算逻辑
水平梯度衡量沿径向的温度变化。计算前，必须先确定每个点在径向上的“内侧邻居”和“外侧邻居”。
前提: 假设所有测温电缆按空间位置可分为若干个同心圆环（如：中心、内环、中环、外环）。
邻居点查找: 对于任意点 P(i, j)，其径向邻居 P(i, m) (外) 和 P(i, n) (内) 必须满足：
位于同一水平层 i 上。
位于与 j 号电缆相邻的圆环上。
与 P(i, j) 的极坐标角度差（角向距离）最小。
计算逻辑根据点 P(i, j) 所在圆环位置分类讨论：
最外环点: 只有内侧邻居 P(i, n)。采用两点差分法。
\text{grad_h}(i, j) = \frac{T(i, j) - T(i, n)}{\text{distance}(P(i, j), P(i, n))}
最内环点 (非中心点): 只有外侧邻居 P(i, m)。采用两点差分法。
\text{grad_h}(i, j) = \frac{T(i, m) - T(i, j)}{\text{distance}(P(i, m), P(i, j))}
中间环点: 同时存在内侧邻居 P(i, n) 和外侧邻居 P(i, m)。采用中心差分法。
\text{grad_h}(i, j) = \frac{T(i, m) - T(i, n)}{\text{distance}(P(i, m), P(i, n))}
中心点 (若有): 中心点在径向上是极点，其 grad_h 定义为0。
2.2 风险合成与数据持久化
2.2.1 点位级风险分数 (trackC_score)
为每个测温点 P(i, j) 计算一个独立的轨道C风险因子 trackC_score。
score
(
i
,
j
)
=
clip
(
w
v
⋅
norm
(
grad_v
(
i
,
j
)
)
+
w
h
⋅
norm
(
grad_h
(
i
,
j
)
)
,
0
,
1
)
score(i,j)=clip(w 
v
​
 ⋅norm(grad_v(i,j))+w 
h
​
 ⋅norm(grad_h(i,j)),0,1)
norm(): 线性归一化函数。将梯度绝对值按预设的“中风险”和“高风险”阈值映射到 区间。例如 norm(G) = (|G| - G_mid) / (G_high - G_mid)。
w_v, w_h: 权重系数。初始推荐值为 w_v = 0.6, w_h = 0.4，因为垂直梯度通常是驱动水汽大规模迁移的主导因素。
clip(): 结果裁剪函数，确保最终分数落在 区间内。
2.2.2 点位级风险等级 (trackC_level)
根据点位风险分数 score(i, j) 离散化为四个风险等级。
风险等级	Level (数值)	分数范围 (score)	建议颜色
高风险	3	score ≥ 0.85	红色
中风险	2	0.5 ≤ score < 0.85	橙色
低风险	1	0.3 ≤ score < 0.5	黄色
无风险	0	score < 0.3	绿色
2.2.3 数据库写入
在n8n工作流的“批量写入风险快照”节点中，必须确保granary_risk_snapshot表的相关字段为每个sensor_id保存其独立计算的值。
3. 数据模型与系统集成
3.1 数据库设计
本功能不新增表结构，但依赖并深度使用以下现有表：
granary_sensors (传感器信息表)
关键字段: id, code, loc (PostGIS PointZ, 包含X,Y,Z三维坐标)。
用途: 提供计算梯度所需的每个传感器的空间位置。
granary_measurements (测量数据时序表)
关键字段: time, sensor_id, temperature。
用途: 提供计算梯度所需的原始温度读数。
granary_risk_snapshot (风险快照结果表)
关键字段: ts, sensor_id (联合主键)。
写入目标字段: temp_grad_v, temp_grad_h, trackc_score, trackc_level。这些字段的值将从“仓级统一值”更新为“点位级独立值”。
3.2 (强烈建议) 性能优化：创建邻居关系视图/表
为避免在每次n8n工作流运行时进行耗时的实时空间几何计算（查找邻居点），建议在数据库中创建一个静态的邻居关系表或物化视图。
建议表名: sensor_neighbor_relations
建议表结构:
| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| sensor_id | integer | 传感器ID (主键) |
| inner_neighbor_id | integer | 径向内侧最近邻居的ID |
| outer_neighbor_id | integer | 径向外侧最近邻居的ID |
| upper_neighbor_id | integer | 垂直上方邻居的ID |
| lower_neighbor_id | integer | 垂直下方邻居的ID |
数据来源: 该表数据可通过一次性的、复杂的PostGIS空间查询生成。在传感器布局不变的情况下，此表无需更新。
使用方式: Compute_TrackC 节点在计算前，先查询此表获取所有点的邻居关系，然后直接根据ID获取温度值进行计算，从而将复杂的空间查询简化为高效的键值查找。
4. 非功能性需求
性能 (Performance): 采用 3.2 节建议的邻居关系表方案后，Compute_TrackC 节点的计算时间应控制在10秒以内（针对约500个测温点）。
可配置性 (Configurability): 所有用于风险计算的阈值（如G_high, G_mid）和权重（w_v, w_h）必须在Compute_TrackC代码的顶部作为常量进行清晰定义，便于后续现场标定和调整。
健壮性 (Robustness): 计算逻辑必须能优雅地处理传感器数据缺失或失效的情况。若一个点的邻居数据缺失，该点的梯度值应被置为NULL，其trackC_score和trackC_level相应地也应置为NULL或安全的默认值0。
5. 验收标准
功能实现:

Compute_TrackC 节点代码已根据本PRD V2.0描述的点位级梯度算法完全重写。

工作流成功运行后，在granary_risk_snapshot表中，同一时间戳（ts）下的不同sensor_id行，其temp_grad_v, temp_grad_h, trackc_score, trackc_level字段必须显示不同的、与其空间位置相符的计算值。
数据准确性:

方向校验: 手动选取一个外环点，验证其grad_h是否为正（或接近0），选取一个内环点，验证其grad_h是否为负（或接近0），以确认“向外为正”的约定已正确实施。
数值校验: 手动选取一个中间环点、一个顶层点，根据数据库原始温度数据，按照本文档中的公式进行人工验算，结果应与系统计算出的grad_v和grad_h值在合理误差（如±5%）内一致。
等级校验: 选取几个分数分别落在0.2、0.4、0.6、0.9附近的点，检查其trackc_level是否正确对应为0、1、2、3。
系统集成:

（若实施）已成功创建并填充sensor_neighbor_relations表（或视图），且Compute_TrackC节点已改为从此表读取邻居关系。

GEMC-结露风险计算V2工作流的整体执行时间未因本次修改而出现不可接受的延长。

三维可视化前端（dashboard.py）能够正确读取并展示点位级的trackc_level（或基于trackc_score进行渲染），形成一幅精细的梯度风险云图。