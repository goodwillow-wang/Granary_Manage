# 大型粮仓结露预警智能系统 - 轨道C（点位级梯度模型）专项技术开发总结报告

| 文档版本 | 状态 | 编制日期 | 编制人 |
|---------|------|---------|-------|
| V1.1 (增强详尽版) | 正式发布 | 2025年9月26日 | 王柳 |

## 修订历史

| 版本 | 主要变更内容 |
|-----|------------|
| V1.1 | - 新增了对`temp_local_spike`和`temp_trend_rt`从需求定义到实现细节的详尽描述<br>- 明确了`trackc_score`最终的四指标融合公式、权重及归一化逻辑<br>- 补充了边界点邻居关系的详细定义 |
| V1.0 | 初始版本，总结了轨道C从宏观到微观的演进，并概述了核心指标的实现 |

## 1. 项目背景与总体概述

### 1.1 项目愿景

本项目致力于构建一套先进、智能的大型粮仓结露预警系统。系统通过融合多维度物理模型与三维可视化技术，为粮库保管员提供从早期风险信号捕捉到直观态势感知的全方位决策支持，最终目标是保障国家粮食存储安全，减少储粮损失。

### 1.2 阶段性成果（本次开发前）

在本次专项开发启动前，系统已具备以下核心能力（阶段 I & II）：

**双轨物理模型:**
- 轨道A (顶空锚定法): 通过计算粮面温度与顶空露点温度的差值 ΔT，实现对表层结露风险的直接判断。
- 轨道B (ERH湿度校验): 通过对比估算空气相对湿度(RH_est)与粮食平衡相对湿度(ERH)，对轨道A的风险等级进行加权修正，提升了对"返潮"风险的识别能力。

**自动化工作流:** 基于n8n平台，实现了从数据采集、风险计算、数据持久化到企业微信告警的完整自动化流程。

**数据中心:** 采用PostgreSQL + TimescaleDB + PostGIS的技术栈，高效存储和查询海量的时序与空间数据。

**三维可视化:** 开发了基于Python Plotly Dash的交互式三维仪表盘，能够以"场"和"点"的形式直观展示粮仓内的温湿度和风险分布。

### 1.3 本次专项开发目标

尽管已有双轨模型，但系统仍缺乏对粮堆内部由水汽迁移（Moisture Migration）引发的深层、早期风险的有效监控手段。温度梯度是驱动水汽迁移的核心动力。因此，本次专项开发（阶段 III）的核心目标是：设计并实现一个全新的、基于点位级温度梯度的独立风险模型（轨道C），并将其无缝集成到现有系统架构中。

## 2. 轨道C：从宏观到微观的演进与最终设计

### 2.1 初始设计（已废弃）

**理念:** 将整个粮仓视为一个整体，计算其顶层与底层的平均温差，得出一个代表全仓的宏观温度梯度。

**问题:** 该方法严重抹平了粮堆内部的局部温度异常，无法定位风险源头，失去了预警价值，在项目初期被果断摒弃。

### 2.2 最终设计：点位级梯度风险模型 (PRD V2.0)

**核心理念:** 粮堆是热力学非均质体，风险诞生于局部。必须为粮仓内的每一个测温点独立计算其所在空间位置的梯度，形成一幅精细的"梯度风险地图"。

**关键指标:** 为每个传感器定义了四个核心梯度指标，并统一存入granary_risk_snapshot表：
- `temp_grad_v` (垂直温度梯度)
- `temp_grad_h` (水平温度梯度)
- `temp_local_spike` (局部突变指标)
- `temp_trend_rt` (短期温度变化率)

## 3. 核心指标实现详解

为确保计算的高效与准确，我们设计并实现了一个关键的"基础设施"——`granary_sensor_neighbor_relations`表。该表通过一个独立的n8n维护工作流进行填充，预先计算并存储了所有传感器之间的全部六个方向（上、下、内、外、同环前、后）的空间邻居关系。所有后续计算都基于此表进行，极大地提升了主工作流的性能。

### 3.1 垂直温度梯度 (`temp_grad_v`)

**物理意义:** 衡量沿测温电缆的垂直温度变化率，反映垂直方向的水汽迁移驱动力。

**方向定义:** 向上为正 (上热下冷为正)。

**计算规则:**
- 中间点 (i): 采用中心差分法 $$ \frac{T_{i+1} - T_{i-1}} { Z_{i+1} - Z_{i-1}}$$
- 边界点 (最底层/最顶层): 严格遵循PRD规定，继承其内侧邻居点（第二层/倒数第二层）已计算出的梯度值，确保了边界数据的物理连续性

**健壮性处理:** 在计算时增加了对邻居温度数据缺失的"降级处理"逻辑，优先使用中心差分，若数据不足则自动降级为两点差分，最大限度地保证了计算的成功率。

### 3.2 水平温度梯度 (`temp_grad_h`)

**物理意义:** 衡量沿筒仓径向的温度变化率，反映从仓心到仓壁（或反向）的水汽迁移驱动力。

**方向定义:** 向外为正 (外热内冷为正)。

**计算规则:**
- 中间环点: 采用中心差分法 
$$ \frac{T_{i+1} - T_{i-1}} { Z_{i+1} - Z_{i-1}}$$ $$
- 最内/最外环点: 采用两点差分法 $$\frac{T_{outer}-T_{self} }{Distance(outer, self)}$$ 或  $$\frac{T_{self}-T_{outer} }{Distance(self, outer)}$$ 
- 中心点: 定义为0

### 3.3 局部突变指标 (`temp_local_spike`)

**需求定义:**

**物理意义:** 衡量一个点与其所有直接相邻邻居之间最剧烈的温度变化率。该指标对发现局部"冷点"或"热点"，特别是传感器故障，具有极高的敏感性和管理价值。

**公式:**
公式: $$ S_{local} = \max\left(\frac{|T_{self} - T_j|}{d_{self,j}}\right) \quad \text{for all neighbors } j $$

**邻居关系定义:**
一个标准的中间点拥有6个邻居：上、下、内（前）、外（后）、同环左、同环右。

边界点的邻居数量会相应减少，例如：
- 外环底部点: 拥有4个邻居（上、内、同环左、同环右）
- 中间环顶部点: 拥有5个邻居（下、内、外、同环左、同环右）

**实现方案:**
1. 在`granary_sensor_neighbor_relations`表中新增`same_ring_neighbor_prev_id`和`same_ring_neighbor_next_id`字段，以存储同环邻居
2. 升级n8n维护工作流，使其能够计算并填充这两个新增字段
3. 在主计算工作流中，Compute_TrackC_V2节点获取一个点的全部6个方向的邻居温度和坐标
4. 通过循环，计算该点与每一个有效邻居之间的梯度，并取其最大值作为`temp_local_spike`

### 3.4 短期温度变化率 (`temp_trend_rt`)

**需求定义:**

**物理意义:** 衡量单个测温点自身温度随时间的变化速率，反映粮温的动态趋势（升温或降温），对发现隐蔽性发热或响应外界气候变化至关重要。

**公式:**
$$ R_t = \frac{T(t) - T(t-\Delta t)}{\Delta t} \quad (\Delta t \geq 6 \text{ hours}) $$

**单位:** ℃ / 日

**实现方案:**
1. 在主计算工作流中，新增一个Postgres节点，专门用于查询历史数据。该节点利用窗口函数，高效地为每一个传感器查找其在当前时间点6小时之前的最近一次温度记录
2. 通过Merge节点，将查询到的前期温度`t_past`和时间`time_past`合并到主数据流中
3. Compute_TrackC_V2节点接收到`t_now`, `time_now`, `t_past`, `time_past`后，进行以下计算：
   - 计算精确的时间差Δt（毫秒）
   - 将Δt从毫秒换算为天
   - 计算 $$ \frac{t_{now} - t_{past}} { \Delta t_{days}} $$ 得到temp_trend_rt

**特别修正:** 解决了当`t_now == t_past`时，因JavaScript"falsy"值陷阱导致结果错误地记为null的BUG，确保结果正确地记为0。

## 4. 综合风险评分与系统集成

### 4.1 综合风险分数 (`trackc_score`)

将上述四个独立的物理指标，通过一个可解释的加权线性模型，融合成一个[0, 1]区间的综合风险分数Fc。这是轨道C的最终输出，全面反映了点位级的梯度风险。

**最终融合公式:**
$$ F_C = \text{clip}\left(w_v \cdot \text{norm}(G_v) + w_h \cdot \text{norm}(\Delta T_h) + w_s \cdot \text{norm}(S_{local}) + w_r \cdot \text{norm}(-R_t),\ 0,\ 1\right) $$


**权重与阈值 (初始配置):**

| 指标 | 变量 | 权重 (w) | 归一化阈值 (mid, high) | 备注 |
|------|------|----------|----------------------|------|
| 垂直梯度 | G_v | 0.4 | 0.2, 0.8 (℃/m) | 核心驱动力 |
| 水平梯度 | ΔT_h | 0.3 | 0.3, 1.0 (℃/m) | |
| 局部突变 | S_local | 0.2 | 0.5, 1.5 (℃/m) | 故障/异常诊断 |
| 降温速率 | -R_t | 0.1 | 0.1, 0.5 (℃/日) | 动态风险 |

**未来展望:** 当前的权重和阈值为基于专家经验的初始设定。代码中已明确注释，这些参数是未来引入AI/机器学习模型进行自动优化和标定的关键特征和超参数。

### 4.2 风险等级 (`trackc_level`)

将连续的风险分数`trackc_score`离散化为四个等级，便于决策和告警。

| 风险等级 | Level | 分数范围 |
|---------|-------|---------|
| 高风险 | 3 | score ≥ 0.85 |
| 中风险 | 2 | 0.5 ≤ score < 0.85 |
| 低风险 | 1 | 0.3 ≤ score < 0.5 |
| 无风险 | 0 | score < 0.3 |

### 4.3 数据库集成

**关键基础设施:** 本次开发的核心成果之一是创建并扩展了`granary_sensor_neighbor_relations`表，使其包含全部六个方向的邻居。该表解耦了复杂的空间关系计算与频繁的风险计算，是系统高性能和可维护性的基石。

**数据写入:** GEMC-结露风险计算V2工作流的最终INSERT语句被全面重构，能够将包含点位级A、B、C所有轨道指标（包括`temp_local_spike`和`temp_trend_rt`）的完整数据，以UPSERT的方式批量写入`granary_risk_snapshot`表，确保了数据的原子性和幂等性。

## 5. 总结与展望

本次专项开发成功地将一个概念性的"温度梯度模型"落地为一个功能完备、逻辑严谨、符合生产实际的点位级风险分析引擎。通过一系列细致的调试与迭代，我们不仅实现了PRD中定义的所有功能，还建立了一套健壮的数据处理流程，为系统的未来发展奠定了坚实的基础。

后续的开发者在阅读本报告后，应能清晰地理解轨道C的每一个指标的来龙去脉、计算细节及其在整个系统中的位置。

**未来工作建议:**
1. **AI/ML集成:** 利用`granary_risk_snapshot`表中积累的丰富特征（`temp_grad_v`, `temp_grad_h`, `delta_t`等），训练机器学习模型（如XGBoost），对权重w进行动态优化，实现更智能的风险评分
2. **可视化增强:** 在三维仪表盘中，为`temp_local_spike`和`temp_trend_rt`开发专门的可视化模式（如异常点高亮、趋势箭头等），为保管员提供更直观的洞察
3. **模型扩展:** 将本次为圆形仓建立的邻居关系计算模式进行扩展，开发适用于平房仓（矩形网格）的邻居计算逻辑，增强系统的普适性