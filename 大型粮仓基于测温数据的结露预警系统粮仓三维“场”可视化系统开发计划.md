# 粮仓三维"场"可视化系统 V3.0 - 开发计划

本计划遵循迭代式增量开发的原则，从最核心的后端数据生成开始，逐步构建前端的可视化与交互功能。共分为四个主要阶段。

## 阶段一：后端核心 - 三维场数据引擎开发 (预计时间: 2-3天)

**目标**: 创建一个独立的、可复用的Python模块，该模块能够接收离散的传感器数据，并输出一个完整的三维网格场数据。这是整个项目的数据基石。

### 任务 1.1: 环境与结构搭建
- 创建一个新的Python文件，例如 `field_generator.py`，用于存放所有数值计算相关的代码
- 引入必要的库：`numpy` (用于高效的数值和数组操作), `pandas` (用于数据处理), `scipy` (用于空间插值), `psycopg2` (用于数据库连接)

### 任务 1.2: 数据接入与预处理
- 编写一个函数，复用 `dashboard.py` 中的数据库连接逻辑，用于获取指定仓库最新的传感器数据（`code`, [x](file://d:\GrainAssitant\lqData\WaterEqb\Design\轨道ABC结果差异的原因分析.docx), [y](file://d:\GrainAssitant\lqData\WaterEqb\Design\crawl_academia_metadata.py), `z`，以及要插值的`value`如temperature）

### 任务 1.3: 空间网格定义
- 编写一个函数，根据粮仓的尺寸（半径、高度），生成一个 50x50x50 的笛卡尔坐标网格
- 实现一个边界检查逻辑，确保只对位于圆柱体粮仓内部的网格点进行插值计算

### 任务 1.4: 核心插值算法实现
- **首要目标**: 实现稳定高效的全局反距离权重法 (IDW) 插值。我们将使用 `scipy.interpolate.griddata` 函数，并设置 `method='cubic'` 或 `'linear'` 作为高性能的初始实现，其效果类似于IDW
- **次要目标 (预留)**: 封装插值逻辑，为未来引入局部Kriging修正（如使用 `pykrige` 库）预留接口

### 任务 1.5: 模块封装与测试
- 将上述功能封装成一个主函数或一个类，例如 `generate_field(warehouse_id, metric)`，输入仓库ID和指标名称，输出一个包含 [x](file://d:\GrainAssitant\lqData\WaterEqb\Design\轨道ABC结果差异的原因分析.docx), [y](file://d:\GrainAssitant\lqData\WaterEqb\Design\crawl_academia_metadata.py), `z`, `value` 列的Pandas DataFrame
- 编写一个 `if __name__ == '__main__':` 块，用于独立测试该模块，确保其能正确连接数据库、生成网格数据并打印出结果样本

## 阶段二：前端基础 - 三维体渲染展示 (预计时间: 2-3天)

**目标**: 将后端生成的三维场数据在Dash应用中成功渲染出来，实现基本的"热力云"效果。

### 任务 2.1: UI布局调整
- 在 `dashboard.py` 中，为新的"场"可视化功能创建一个新的UI容器，例如使用 `dcc.Tabs` 将现有的"点位视图"和新的"场视图"分开
- 在"场视图"标签下，放置一个新的 `dcc.Graph` 组件

### 任务 2.2: 数据流打通
- 修改Dash的回调函数，当用户切换到"场视图"时，调用阶段一开发的 `field_generator.py` 模块来获取三维场数据

### 任务 2.3: 实现体渲染
- 使用 `plotly.graph_objects.Volume` 来创建三维体渲染图
- 将获取到的网格数据 ([x](file://d:\GrainAssitant\lqData\WaterEqb\Design\轨道ABC结果差异的原因分析.docx), [y](file://d:\GrainAssitant\lqData\WaterEqb\Design\crawl_academia_metadata.py), `z`, `value`) 传递给 `go.Volume` 的相应参数

### 任务 2.4: 视觉效果调优
- 配置 `colorscale` 和 `opacityscale`，创建从低温（透明、蓝色）到高温（不透明、红色）的平滑视觉过渡，使其看起来直观且美观

## 阶段三：前端核心 - 交互式剖面分析 (预计时间: 3-5天)

**目标**: 实现PRD中定义的核心交互功能——通过滑块动态、实时地在三维视图中展示各种剖面。

### 任务 3.1: 增加交互控件
- 在侧边栏添加"剖面分析"的UI控件：模式选择（正常、横剖面、纵剖面、环剖面）的单选按钮，以及与各模式联动的滑块

### 任务 3.2: 开发横剖面功能 (Axis-Aligned)
- 更新回调函数以响应"高度滑块"
- 研究并使用 `go.Volume` 的 `slices` 属性或叠加 `go.Surface` 对象，根据滑块的值动态生成并显示一个水平切面

### 任务 3.3: 开发纵剖面与环剖面功能 (Non-Axis-Aligned)
- 这是一个技术难点。需要编写额外的几何计算函数：
  - **纵剖面**: 根据"角度滑块"，计算出该角度下径向切片平面的所有点坐标
  - **环剖面**: 根据"半径滑块"，计算出该半径下圆柱侧面的所有点坐标
- 对计算出的剖面上的点，再次调用插值函数（或从已有网格数据中进行二次插值）以获取其value值
- 使用 `go.Surface` 将这些计算出的剖面渲染为着色的曲面，并叠加在三维视图中

### 任务 3.4: 性能优化
- 确保拖动滑块时的交互是流畅的。如果卡顿，则需要优化回调逻辑，例如使用客户端回调或优化数据传输

## 阶段四：整合与完善 (预计时间: 2-3天)

**目标**: 完成所有功能的整合，进行最终的优化、测试和文档编写。

### 任务 4.1: 实现场类型切换
- 添加"温度场/风险场"的切换下拉菜单
- 将回调函数与此菜单关联，使其能够向后端请求不同类型的场数据并更新视图

### 任务 4.2: UI/UX润色
- 添加动态标题、图例、以及显示当前剖面位置的文本标签
- 统一所有UI组件的风格，使其与现有仪表盘保持一致

### 任务 4.3: 代码审查与文档
- 对所有新增代码进行审查，特别是 `field_generator.py` 中的数值计算部分，确保注释清晰、准确
- 更新项目的 `README.md` 或相关技术文档，说明新功能的使用方法和技术实现

### 任务 4.4: 全面测试
- 在不同浏览器上测试所有交互功能
- 测试边界情况（例如，无数据、数据异常等）